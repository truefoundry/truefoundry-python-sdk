# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.budget_usage_response import BudgetUsageResponse
from ..types.gateway_budget import GatewayBudget
from ..types.list_budgets_response import ListBudgetsResponse
from .raw_client import AsyncRawGatewayBudgetsClient, RawGatewayBudgetsClient
from .types.create_or_update_budget_request_manifest import CreateOrUpdateBudgetRequestManifest
from .types.list_gateway_budgets_request_type import ListGatewayBudgetsRequestType

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class GatewayBudgetsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawGatewayBudgetsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawGatewayBudgetsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawGatewayBudgetsClient
        """
        return self._raw_client

    def list(
        self,
        *,
        type: typing.Optional[ListGatewayBudgetsRequestType] = None,
        team_name: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ListBudgetsResponse:
        """
        List the gateway budgets the caller can read within the tenant.

        Parameters
        ----------
        type : typing.Optional[ListGatewayBudgetsRequestType]
            Filter by budget type.

        team_name : typing.Optional[str]
            Human-readable name of the team owning the budget. Only matches team budgets.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListBudgetsResponse
            List of budgets.

        Examples
        --------
        from truefoundry_sdk import TrueFoundry

        client = TrueFoundry(
            api_key="YOUR_API_KEY",
            base_url="https://yourhost.com/path/to/api",
        )
        client.gateway_budgets.list()
        """
        _response = self._raw_client.list(type=type, team_name=team_name, request_options=request_options)
        return _response.data

    def create_or_update(
        self,
        *,
        manifest: CreateOrUpdateBudgetRequestManifest,
        dry_run: typing.Optional[bool] = False,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> GatewayBudget:
        """
        Creates or updates a budget manifest for the tenant. Budgets are upserted by `manifest.name` (unique per tenant).

        Parameters
        ----------
        manifest : CreateOrUpdateBudgetRequestManifest
            The budget manifest. Must match either the TenantBudgetConfig or TeamBudgetConfig schema.

        dry_run : typing.Optional[bool]
            When `true`, validates the manifest without persisting changes.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GatewayBudget
            The created or updated budget.

        Examples
        --------
        from truefoundry_sdk import (
            BudgetV2AppliesToAggregate,
            BudgetV2Limits,
            TenantBudgetConfig,
            TenantBudgetConfigMode,
            TrueFoundry,
        )

        client = TrueFoundry(
            api_key="YOUR_API_KEY",
            base_url="https://yourhost.com/path/to/api",
        )
        client.gateway_budgets.create_or_update(
            manifest=TenantBudgetConfig(
                name="name",
                limits=BudgetV2Limits(),
                applies_to=BudgetV2AppliesToAggregate(),
                mode=TenantBudgetConfigMode.ENFORCE,
            ),
        )
        """
        _response = self._raw_client.create_or_update(
            manifest=manifest, dry_run=dry_run, request_options=request_options
        )
        return _response.data

    def get_my_usage(self, *, request_options: typing.Optional[RequestOptions] = None) -> BudgetUsageResponse:
        """
        Returns every per-user budget that currently applies to the caller, with current usage per period.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        BudgetUsageResponse
            The caller-scoped budget usage.

        Examples
        --------
        from truefoundry_sdk import TrueFoundry

        client = TrueFoundry(
            api_key="YOUR_API_KEY",
            base_url="https://yourhost.com/path/to/api",
        )
        client.gateway_budgets.get_my_usage()
        """
        _response = self._raw_client.get_my_usage(request_options=request_options)
        return _response.data

    def simulate(
        self,
        *,
        user: typing.Optional[str] = OMIT,
        virtual_account: typing.Optional[str] = OMIT,
        team: typing.Optional[str] = OMIT,
        teams: typing.Optional[typing.Sequence[str]] = OMIT,
        model: typing.Optional[str] = OMIT,
        metadata: typing.Optional[typing.Dict[str, str]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> BudgetUsageResponse:
        """
        Returns the budgets that would apply to a hypothetical user/team/model/metadata selection, with current usage.

        Parameters
        ----------
        user : typing.Optional[str]
            User email to simulate as.

        virtual_account : typing.Optional[str]
            Virtual account slug to simulate as. Use this instead of `user` for virtual-account budgets.

        team : typing.Optional[str]
            Single team to simulate membership of. Use `teams` to pass multiple teams.

        teams : typing.Optional[typing.Sequence[str]]
            All teams the simulated user belongs to. Merged with `team` when both are provided.

        model : typing.Optional[str]
            Model to simulate.

        metadata : typing.Optional[typing.Dict[str, str]]
            Request metadata key/value pairs to simulate.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        BudgetUsageResponse
            The budgets matching the selection.

        Examples
        --------
        from truefoundry_sdk import TrueFoundry

        client = TrueFoundry(
            api_key="YOUR_API_KEY",
            base_url="https://yourhost.com/path/to/api",
        )
        client.gateway_budgets.simulate()
        """
        _response = self._raw_client.simulate(
            user=user,
            virtual_account=virtual_account,
            team=team,
            teams=teams,
            model=model,
            metadata=metadata,
            request_options=request_options,
        )
        return _response.data

    def get(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> GatewayBudget:
        """
        Returns a single budget by id.

        Parameters
        ----------
        id : str
            The budget id to retrieve or delete.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GatewayBudget
            The requested budget.

        Examples
        --------
        from truefoundry_sdk import TrueFoundry

        client = TrueFoundry(
            api_key="YOUR_API_KEY",
            base_url="https://yourhost.com/path/to/api",
        )
        client.gateway_budgets.get(
            id="id",
        )
        """
        _response = self._raw_client.get(id, request_options=request_options)
        return _response.data

    def delete(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Permanently deletes a budget manifest.

        Parameters
        ----------
        id : str
            The budget id to retrieve or delete.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from truefoundry_sdk import TrueFoundry

        client = TrueFoundry(
            api_key="YOUR_API_KEY",
            base_url="https://yourhost.com/path/to/api",
        )
        client.gateway_budgets.delete(
            id="id",
        )
        """
        _response = self._raw_client.delete(id, request_options=request_options)
        return _response.data

    def get_leaderboard(
        self, id: str, *, limit: typing.Optional[float] = None, request_options: typing.Optional[RequestOptions] = None
    ) -> BudgetUsageResponse:
        """
        Returns the top spenders for a budget in its applies_to dimension, for the configured period. Aggregate budgets return a single whole-budget entity (entity: null).

        Parameters
        ----------
        id : str
            The budget id to retrieve or delete.

        limit : typing.Optional[float]
            Maximum number of entries to return. Defaults to 500 and is capped at 500.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        BudgetUsageResponse
            The budget leaderboard.

        Examples
        --------
        from truefoundry_sdk import TrueFoundry

        client = TrueFoundry(
            api_key="YOUR_API_KEY",
            base_url="https://yourhost.com/path/to/api",
        )
        client.gateway_budgets.get_leaderboard(
            id="id",
            limit=50.0,
        )
        """
        _response = self._raw_client.get_leaderboard(id, limit=limit, request_options=request_options)
        return _response.data


class AsyncGatewayBudgetsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawGatewayBudgetsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawGatewayBudgetsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawGatewayBudgetsClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        type: typing.Optional[ListGatewayBudgetsRequestType] = None,
        team_name: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ListBudgetsResponse:
        """
        List the gateway budgets the caller can read within the tenant.

        Parameters
        ----------
        type : typing.Optional[ListGatewayBudgetsRequestType]
            Filter by budget type.

        team_name : typing.Optional[str]
            Human-readable name of the team owning the budget. Only matches team budgets.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListBudgetsResponse
            List of budgets.

        Examples
        --------
        import asyncio

        from truefoundry_sdk import AsyncTrueFoundry

        client = AsyncTrueFoundry(
            api_key="YOUR_API_KEY",
            base_url="https://yourhost.com/path/to/api",
        )


        async def main() -> None:
            await client.gateway_budgets.list()


        asyncio.run(main())
        """
        _response = await self._raw_client.list(type=type, team_name=team_name, request_options=request_options)
        return _response.data

    async def create_or_update(
        self,
        *,
        manifest: CreateOrUpdateBudgetRequestManifest,
        dry_run: typing.Optional[bool] = False,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> GatewayBudget:
        """
        Creates or updates a budget manifest for the tenant. Budgets are upserted by `manifest.name` (unique per tenant).

        Parameters
        ----------
        manifest : CreateOrUpdateBudgetRequestManifest
            The budget manifest. Must match either the TenantBudgetConfig or TeamBudgetConfig schema.

        dry_run : typing.Optional[bool]
            When `true`, validates the manifest without persisting changes.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GatewayBudget
            The created or updated budget.

        Examples
        --------
        import asyncio

        from truefoundry_sdk import (
            AsyncTrueFoundry,
            BudgetV2AppliesToAggregate,
            BudgetV2Limits,
            TenantBudgetConfig,
            TenantBudgetConfigMode,
        )

        client = AsyncTrueFoundry(
            api_key="YOUR_API_KEY",
            base_url="https://yourhost.com/path/to/api",
        )


        async def main() -> None:
            await client.gateway_budgets.create_or_update(
                manifest=TenantBudgetConfig(
                    name="name",
                    limits=BudgetV2Limits(),
                    applies_to=BudgetV2AppliesToAggregate(),
                    mode=TenantBudgetConfigMode.ENFORCE,
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create_or_update(
            manifest=manifest, dry_run=dry_run, request_options=request_options
        )
        return _response.data

    async def get_my_usage(self, *, request_options: typing.Optional[RequestOptions] = None) -> BudgetUsageResponse:
        """
        Returns every per-user budget that currently applies to the caller, with current usage per period.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        BudgetUsageResponse
            The caller-scoped budget usage.

        Examples
        --------
        import asyncio

        from truefoundry_sdk import AsyncTrueFoundry

        client = AsyncTrueFoundry(
            api_key="YOUR_API_KEY",
            base_url="https://yourhost.com/path/to/api",
        )


        async def main() -> None:
            await client.gateway_budgets.get_my_usage()


        asyncio.run(main())
        """
        _response = await self._raw_client.get_my_usage(request_options=request_options)
        return _response.data

    async def simulate(
        self,
        *,
        user: typing.Optional[str] = OMIT,
        virtual_account: typing.Optional[str] = OMIT,
        team: typing.Optional[str] = OMIT,
        teams: typing.Optional[typing.Sequence[str]] = OMIT,
        model: typing.Optional[str] = OMIT,
        metadata: typing.Optional[typing.Dict[str, str]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> BudgetUsageResponse:
        """
        Returns the budgets that would apply to a hypothetical user/team/model/metadata selection, with current usage.

        Parameters
        ----------
        user : typing.Optional[str]
            User email to simulate as.

        virtual_account : typing.Optional[str]
            Virtual account slug to simulate as. Use this instead of `user` for virtual-account budgets.

        team : typing.Optional[str]
            Single team to simulate membership of. Use `teams` to pass multiple teams.

        teams : typing.Optional[typing.Sequence[str]]
            All teams the simulated user belongs to. Merged with `team` when both are provided.

        model : typing.Optional[str]
            Model to simulate.

        metadata : typing.Optional[typing.Dict[str, str]]
            Request metadata key/value pairs to simulate.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        BudgetUsageResponse
            The budgets matching the selection.

        Examples
        --------
        import asyncio

        from truefoundry_sdk import AsyncTrueFoundry

        client = AsyncTrueFoundry(
            api_key="YOUR_API_KEY",
            base_url="https://yourhost.com/path/to/api",
        )


        async def main() -> None:
            await client.gateway_budgets.simulate()


        asyncio.run(main())
        """
        _response = await self._raw_client.simulate(
            user=user,
            virtual_account=virtual_account,
            team=team,
            teams=teams,
            model=model,
            metadata=metadata,
            request_options=request_options,
        )
        return _response.data

    async def get(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> GatewayBudget:
        """
        Returns a single budget by id.

        Parameters
        ----------
        id : str
            The budget id to retrieve or delete.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GatewayBudget
            The requested budget.

        Examples
        --------
        import asyncio

        from truefoundry_sdk import AsyncTrueFoundry

        client = AsyncTrueFoundry(
            api_key="YOUR_API_KEY",
            base_url="https://yourhost.com/path/to/api",
        )


        async def main() -> None:
            await client.gateway_budgets.get(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get(id, request_options=request_options)
        return _response.data

    async def delete(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Permanently deletes a budget manifest.

        Parameters
        ----------
        id : str
            The budget id to retrieve or delete.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from truefoundry_sdk import AsyncTrueFoundry

        client = AsyncTrueFoundry(
            api_key="YOUR_API_KEY",
            base_url="https://yourhost.com/path/to/api",
        )


        async def main() -> None:
            await client.gateway_budgets.delete(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete(id, request_options=request_options)
        return _response.data

    async def get_leaderboard(
        self, id: str, *, limit: typing.Optional[float] = None, request_options: typing.Optional[RequestOptions] = None
    ) -> BudgetUsageResponse:
        """
        Returns the top spenders for a budget in its applies_to dimension, for the configured period. Aggregate budgets return a single whole-budget entity (entity: null).

        Parameters
        ----------
        id : str
            The budget id to retrieve or delete.

        limit : typing.Optional[float]
            Maximum number of entries to return. Defaults to 500 and is capped at 500.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        BudgetUsageResponse
            The budget leaderboard.

        Examples
        --------
        import asyncio

        from truefoundry_sdk import AsyncTrueFoundry

        client = AsyncTrueFoundry(
            api_key="YOUR_API_KEY",
            base_url="https://yourhost.com/path/to/api",
        )


        async def main() -> None:
            await client.gateway_budgets.get_leaderboard(
                id="id",
                limit=50.0,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_leaderboard(id, limit=limit, request_options=request_options)
        return _response.data
