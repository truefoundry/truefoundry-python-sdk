# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel


class TaskSparkImage(UniversalBaseModel):
    """
    Describes a pre-built Spark image for a task. Use this when your image already contains
    all workflow code, dependencies (including truefoundry[workflow,spark]), and required JARs.
    This skips the Docker build phase entirely.
    """

    type: typing.Literal["task-spark-image"] = pydantic.Field(default="task-spark-image")
    """
    +uiType=Hidden
    +value=task-spark-image
    """

    spark_version: str = pydantic.Field(default="3.5.2")
    """
    Spark version should match the spark version installed in the image.
    """

    container_image: str = pydantic.Field()
    """
    The container image URI of the pre-built Spark image. The image must contain:
    1. All workflow source code at /app (or appropriate PYTHONPATH)
    2. truefoundry[workflow,spark] package installed
    3. PySpark matching the spark_version
    4. Hadoop AWS/GCS/Azure JARs if using cloud storage
    """

    docker_registry: typing.Optional[str] = pydantic.Field(default=None)
    """
    FQN of the container registry. If you can't find your registry here,
    add it through the [Integrations](/integrations?tab=docker-registry) page
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow")  # type: ignore # Pydantic v2
    else:

        class Config:
            smart_union = True
            extra = pydantic.Extra.allow
