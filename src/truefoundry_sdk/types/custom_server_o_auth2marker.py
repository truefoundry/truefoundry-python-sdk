# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .custom_server_o_auth2marker_grant_type import CustomServerOAuth2MarkerGrantType


class CustomServerOAuth2Marker(UniversalBaseModel):
    """
    The Gateway attaches an access token acquired from the agent's configured identity provider. The credentials live in the v2 manifest and are deliberately absent here, so this manifest can be synced to the Gateway without carrying a client secret.
    """

    type: typing.Literal["oauth2"] = pydantic.Field(default="oauth2")
    """
    +value=oauth2
    """

    grant_type: CustomServerOAuth2MarkerGrantType = pydantic.Field()
    """
    The OAuth2 flow the Gateway uses to acquire the token.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow")  # type: ignore # Pydantic v2
    else:

        class Config:
            smart_union = True
            extra = pydantic.Extra.allow
