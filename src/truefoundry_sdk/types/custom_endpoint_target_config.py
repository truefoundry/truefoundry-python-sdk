# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel


class CustomEndpointTargetConfig(UniversalBaseModel):
    """
    Per-endpoint load balancing settings for a load-balanced custom-endpoint provider account. Only effective when the parent account has routing_type set to "weight" or "priority".
    """

    weight: typing.Optional[int] = pydantic.Field(default=None)
    """
    Relative weight for routing decisions (higher values mean more traffic). Weights across all endpoints in the account must sum to 100.
    """

    priority: typing.Optional[int] = pydantic.Field(default=None)
    """
    Priority for the endpoint, Lower the number, higher the priority (0 is the highest priority)
    """

    fallback_status_codes: typing.Optional[typing.List[str]] = pydantic.Field(default=None)
    """
    Status Codes for which the request will fall back to other endpoints. If the status code is not present in fallback_status_codes, it fails immediately.
    """

    fallback_candidate: typing.Optional[bool] = pydantic.Field(default=True)
    """
    Whether this endpoint is a fallback candidate. If set to false, this endpoint will not be considered as a fallback option for the account.
    """

    metadata_match: typing.Optional[typing.Dict[str, str]] = pydantic.Field(default=None)
    """
    Optional metadata key-value pairs that must match incoming request metadata headers for this endpoint to be considered for routing.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow")  # type: ignore # Pydantic v2
    else:

        class Config:
            smart_union = True
            extra = pydantic.Extra.allow
