# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .sticky_session_identifier import StickySessionIdentifier


class ComplexityStickyRouting(UniversalBaseModel):
    """
    Sticky routing for complexity-based routing. A session is pinned to the tier/target chosen on its first classified turn for up to ttl_seconds (5 minutes to 1 hour).
    """

    ttl_seconds: int = pydantic.Field()
    """
    Sticky mapping TTL in seconds. Minimum 5 minutes, maximum 1 hour.
    """

    session_identifiers: typing.List[StickySessionIdentifier] = pydantic.Field()
    """
    List of identifier selectors used to build sticky key
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow")  # type: ignore # Pydantic v2
    else:

        class Config:
            smart_union = True
            extra = pydantic.Extra.allow
