# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .agent_channel_slack_auth import AgentChannelSlackAuth


class AgentChannelSlackConfig(UniversalBaseModel):
    """
    Reach the agent from a Slack app (one Slack app per channel).
    """

    type: typing.Literal["slack"] = pydantic.Field(default="slack")
    """
    +uiType=Hidden
    +value=slack
    """

    app_id: str = pydantic.Field()
    """
    The Slack app id (api_app_id). Inbound events are routed to this channel by app id.
    """

    signing_secret: str = pydantic.Field()
    """
    Slack app signing secret, used to verify inbound event request signatures. App-level; common to all auth methods.
    """

    auth: AgentChannelSlackAuth

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow")  # type: ignore # Pydantic v2
    else:

        class Config:
            smart_union = True
            extra = pydantic.Extra.allow
