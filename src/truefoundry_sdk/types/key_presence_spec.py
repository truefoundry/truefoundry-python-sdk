# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel


class KeyPresenceSpec(UniversalBaseModel):
    """
    Only the key is checked — any value will pass. Once configured, the key must be present in the metadata header.
    """

    required: typing.Optional[typing.Literal[True]] = pydantic.Field(default=None)
    """
    +uiType=Hidden
    +value=true
    A presence rule that doesn't require the key is a no-op, so this is fixed to true.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow")  # type: ignore # Pydantic v2
    else:

        class Config:
            smart_union = True
            extra = pydantic.Extra.allow
